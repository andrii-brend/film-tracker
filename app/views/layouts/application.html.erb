<!DOCTYPE html>
<html>
  <head>
    <title>MovieApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  
  <div class="p-3 bg-dark text-white">
    <div class="container margin:0 5%;">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <li><span class="nav-link px-2 text-secondary"><%=link_to "List Movies", movies_path, {:style=>'color:#FFFFFF; text-decoration: none;'}%><span></li>
          <li><span class="nav-link px-2 text-secondary"><%= link_to "New movie", new_movie_path, {:style=>'color:#FFFFFF; text-decoration: none;'} %><span></li>
        </ul>
        
        <% if user_signed_in? %>
          <span>Hello, <%= current_user.email %></span>
          <button type="button" class="btn btn-warning" style="margin-left:5px"><%= link_to "Log out", destroy_user_session_path,data: {"turbo-method": :delete}, class: "link-secondary smooth", style:  "color:#000000; text-decoration: none;"%></button>
        <% else %>
          <button type="button" class="btn btn-outline-light me-2"><%= link_to 'Sign in', new_user_session_path, {:style=>'color:#FFFFFF; text-decoration: none;'} %></button> <button type="button" class="btn btn-warning"><%= link_to 'Sign up', new_user_registration_path, {:style=>'color:#000000; text-decoration: none;'} %></button>
        <% end %>
        </div>
      </div>
  </div>
  <div style="margin:0 5%;">
  <div class="text-center">
    <p class="notice fw-bold"><%= notice %></p>
    <p class="alert fw-bold"><%= alert %></p>
  </div>
    <%= yield %>
  </div>
  <footer class="footer mt-auto py-3 bg-dark">
    <div class="container">
      <a href="https://github.com/andrii-brend/film-tracker" style="text-decoration:none; color:#FFFFFF">Link to GitHub</a>
    </div>
  </footer>
  </body>
</html>
